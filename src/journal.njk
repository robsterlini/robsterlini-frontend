---
metaTitle: Journal
metaDescription: Rob Sterlini’s journal entries about anything and everything.
heroTitle: Entries about anything and everything.
layout: default
sitemapPriority: 0.8
nav: journal
customCss: pages/journal.scss
tag: journal
eleventyComputed:
  heroHeader: "{{ metaTitle }}"
---

{% from "components/hero.html" import hero %}

{{ hero(heroTitle, header='Sporadic ramblings') }}

<main class="main journal">
  <div class="group">
    {% for entry in collections.entries %}
      {% if loop.index == 1 %}
        <div class="journal-latest">
          {% journalEntry entry, "h2", "Most recent entry", true, true %}
        </div>

        <div class="journal-older">
          <h2 class="lead">A few older&nbsp;entries</h2>
          <ul class="list--block">
      {% endif %}

      {% if loop.index > 1 %}
            <li>
              {% journalEntryShort entry %}
            </li>
      {% endif %}

      {% if loop.last %}
          </ul>
        </div>
      {% endif %}
    {% endfor %}
  </div>
</main>
