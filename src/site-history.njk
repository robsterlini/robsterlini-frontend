---
metaTitle: A site’s history
layout: default
sitemap_priority: 0.8
nav: about
customCss: pages/site-history.scss
heroTitle: "The iterative life of a personal website"
eleventyComputed:
  heroHeader: "{{ metaTitle }}"
---

{% from "components/hero.html" import hero %}

{% set children %}<p><a href="/">robsterlini.co.uk</a> has gone through a few (a lot) of revisions over time, and with some poking and prodding I’ve been able to rescue a few notable years. They’re to be consumed as seen, no fixes (other than ensuring the builds ran) were made, so be kind while you’re judging me!</p>{% endset %}

{{ hero(heroTitle, header=heroHeader, children=children | safe) }}

<main class="main site-history">
  <div class="group group--thin links--external">
    {% for year in siteHistory.order %}
      <div class="site-history__item">
        {% set yearData = siteHistory.years[year] %}
        {% if yearData.image %}
          {% set imageAlt %}A screenshot from the {{ year }} build{% endset %}
          {% figureInset
            yearData.image,
            "1280x720",
            imageAlt
          %}
        {% endif %}
        <h2 id="{{ year }}" class="h2">{% if yearData.title %}<span class="p block lead">{{ year }}</span> {{ yearData.title | safe }}{% else %}{{ year }}{% endif %}{% anchor year %}</h2>
        {% if yearData.description or yearData.stack or yearData.type %}<p>
          {% if yearData.stack or yearData.type %}<strong class="s">{% if yearData.stack %}<span class="sc">Stack:</span> {{ yearData.stack }}{% endif %}{% if yearData.stack and yearData.type %}; {% endif %}{% if yearData.type %}<span class="sc">Type:</span> {{ yearData.type }}{% endif %}.</strong>{% endif %}{% if yearData.description %}&ensp;{{ yearData.description | safe }}{% endif %}
        </p>{% endif %}
        <ul class="list">
          {% if yearData.link %}<li><a href="{{ yearData.link }}" target="_blank" rel="noopener">{{ yearData.label }}</a></li>{% endif %}
        </ul>
      </div>
    {% endfor %}
  </div>
</main>
